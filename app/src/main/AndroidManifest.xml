<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:dist="http://schemas.android.com/apk/distribution"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Instant App -->
    <dist:module dist:instant="true" />

    <!-- Features -->
    <uses-feature android:name="android.hardware.camera" />

    <!-- Permissions -->
    <uses-permission android:name="android.permission.CAMERA" />
    <uses-permission android:name="android.permission.VIBRATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name=".DrowsyGuardApplication"
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:taskAffinity=""
        android:theme="@style/Theme.Material3.DayNight.NoActionBar"
        android:usesCleartextTraffic="true"
        tools:ignore="AllowBackup">

        <!-- Jetpack Startup 初始化器 -->
        <provider
            android:name="androidx.startup.InitializationProvider"
            android:authorities="${applicationId}.androidx-startup"
            android:exported="false"
            tools:node="merge">
            <meta-data
                android:name="com.patrick.main.StartupInitializer"
                android:value="androidx.startup" />
        </provider>

        <!-- 先登入頁（成為 Launcher） -->
        <activity
            android:name="drivesafe.LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!-- 主流程頁（非 Launcher） -->
        <activity
            android:name="com.patrick.main.MainActivity"
            android:exported="false"
            android:configChanges="orientation|screenSize|screenLayout|keyboardHidden">
            <!-- Notch 支援 -->
            <meta-data
                android:name="android.notch_support"
                android:value="true" />
        </activity>

        <!-- 其餘頁面（非 Launcher） -->
        <activity android:name="drivesafe.RegisterActivity"        android:exported="false" />
        <activity android:name="drivesafe.ForgotPasswordActivity"  android:exported="false" />
        <activity android:name="drivesafe.HistoryActivity"         android:exported="false" />
        <activity android:name="drivesafe.ChartActivity"           android:exported="false" />
        <activity android:name="drivesafe.SettingsActivity"        android:exported="false" />
        <activity android:name="drivesafe.AdminActivity"           android:exported="false" />
        <activity android:name="drivesafe.AccountManageActivity"   android:exported="false" />
        <activity android:name="drivesafe.EditProfileActivity"     android:exported="false" />
        <activity android:name="drivesafe.ProfileActivity"         android:exported="false" />

    </application>
</manifest>
